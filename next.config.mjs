/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'github.com',
            'via.placeholder.com',
            'www.instagram.com',
            'code-wave-lemon.vercel.app',
            'instagram.com',
        ],
    },
}

export default nextConfig
